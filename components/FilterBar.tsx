import React from 'react';   import { View, TouchableOpacity, Text, StyleSheet } from 'react-native';      type Filter = 'all' | 'active' | 'completed';      interface FilterBarProps {     currentFilter: Filter;     onFilterChange: (filter: Filter) => void;     counts: { all: number; active: number; completed: number };   }      export const FilterBar: React.FC<FilterBarProps> = ({     currentFilter, onFilterChange, counts   }) => {     const filters: { key: Filter; label: string }[] = [       { key: 'all', label: `Semua (${counts.all})` },       { key: 'active', label: `Aktif (${counts.active})` },       { key: 'completed', label: `Selesai (${counts.completed})` },     ];        return (       <View style={styles.container}>         {filters.map(({ key, label }) => (           <TouchableOpacity             key={key}             style={[styles.tab, currentFilter === key && styles.activeTab]}             onPress={() => onFilterChange(key)}           >             <Text style={[styles.label, currentFilter === key && styles.activeLabel]}>               {label}  </Text>           </TouchableOpacity>         ))}       </View>     );   };      const styles = StyleSheet.create({     container: { flexDirection: 'row', backgroundColor: '#E2E8F0',       borderRadius: 10, padding: 4, marginBottom: 12 },     tab: { flex: 1, paddingVertical: 8, alignItems: 'center', borderRadius: 8 },     activeTab: { backgroundColor: '#1A56DB' },     label: { fontSize: 13, fontWeight: '600', color: '#64748B' },     activeLabel: { color: 'FFFFFF' },   }); 